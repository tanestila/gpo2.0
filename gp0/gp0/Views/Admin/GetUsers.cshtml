@model IEnumerable<gp0.ViewModels.UserView>
@{
    ViewData["Title"] = "GetUser";
    Layout = "AdminLayout";
}

<div>
    @foreach (var user in Model)
    {
        <p>@user.full_name</p>
        <p>@user.email</p>
        <p>@user.login</p>
        <hr />
    }
</div>

